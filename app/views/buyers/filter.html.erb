<div class = "container"> 
<div class="row">
    <% if @properties.present? %>
    <% @properties.each do |p|%>
     <div class="card" style="width: 18rem; background-color: #cedae9;">
     <div class="card-body">
     <% if p.avatar.attached? %>
      <%= image_tag p.avatar, height: "150px" %>
    <% end %>
   <h5 class="card-title"><%= p.title%></h5>
    <p class="card-text">
        <%= p.location %> <br>
        <%= p.rate %><br>
        <%= p.area %>
    </p>
    <p> Created by: <%= p.user.name.capitalize %> </p>
    <%= link_to "View", property_path(p), class:"btn btn-primary"%>
    </div>
    </div>
  
<% end %>
</div>
</div>

<%= link_to 'Back' ,buyers_index_path %>